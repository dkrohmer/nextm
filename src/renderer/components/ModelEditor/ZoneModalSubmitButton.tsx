import React from 'react';
import { Form } from 'semantic-ui-react';

const ZoneModalSubmitButton: React.FC = () => {
  return (
    <Form.Button primary type="submit">
      Submit
    </Form.Button>
  );
};

export default ZoneModalSubmitButton;
