import React from 'react';
import { Form } from 'semantic-ui-react';

const ExportModalSubmit: React.FC = () => {
  /**
   * tsx
   */
  return (
    <Form.Button primary type="submit">
      Export
    </Form.Button>
  );
};

export default ExportModalSubmit;
