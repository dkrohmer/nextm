import React from 'react';
import { Form } from 'semantic-ui-react';

const DataflowModalSubmitButton: React.FC = () => {
  /**
   * tsx
   */
  return (
    <Form.Button primary type="submit">
      Submit
    </Form.Button>
  );
};

export default DataflowModalSubmitButton;
